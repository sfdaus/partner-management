apiVersion: v1
kind: Service
metadata:
  name: partner-management
  namespace: prakarsa
  labels:
    app: partner-management
spec:
  type: ClusterIP
  selector:
    app: partner-management
  ports:
    - name: http
      port: 80
      targetPort: 8080